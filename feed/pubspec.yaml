name: feed

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # MICROFRONTENDS
  dependencies:
    path: ../dependencies

  design_system:
    path: ../design_system

  shared:
    path: ../shared

dev_dependencies:
  faker: ^2.0.0
  mocktail: ^0.3.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1